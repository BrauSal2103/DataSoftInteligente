import { useRef } from 'react'; import Button from '../ui/Button';
export default ({file,onPick,onUpload,status}:{file:File|null;onPick:(f:File|null)=>void;onUpload:()=>void;status:string})=>{const ref=useRef<HTMLInputElement>(null);return <div className='space-y-3'><input ref={ref} className='hidden' type='file' accept='.json,.csv' onChange={(e)=>onPick(e.target.files?.[0]??null)}/><div onClick={()=>ref.current?.click()} className='cursor-pointer rounded-2xl border-2 border-dashed border-[#1F2937] bg-[#18181B] p-10 text-center'>Arrastra tu archivo aquí o haz clic para seleccionar {file&&`(${file.name})`}</div><Button onClick={onUpload} disabled={!file}>Cargar dataset</Button><p className='text-[#CBD5E1]'>{status}</p></div>}
